#include<iostream>
using namespace std;
int main()
{
    int a,b;
    cin>>a;
    cin>>b;
    if(a>b)
    cout<<"a greater than b";
    else if(a=b)
    cout<<" Both are equal";
    else
    cout<<"b greater than a";
}